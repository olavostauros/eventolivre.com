/* Every design-system component the site uses passes through here, so the
   deep import paths live in one file when the package grows an exports map. */
export { Button } from "@evento-livre/design-system/src/components/primitives/Button/index.ts";
export { Link } from "@evento-livre/design-system/src/components/primitives/Link/index.ts";
export { Text } from "@evento-livre/design-system/src/components/primitives/Text/index.ts";
